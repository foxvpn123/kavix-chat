rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Catch-all wide access for rapid development and mobile (APK) compatibility.
    // Handles are validated on the client and stored in Firestore for a unique ID system.
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
